## Questão 17

### Resposta

O único que não funciona é a letra **d)**, por não seguir a regra do `()` necessária no `printf`. O resto das letras funciona porque o compilador ignora os espaços em branco.